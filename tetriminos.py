class Tetrimino(object):
    def __init__(self, shape, initial_rotation, x, y):
        self.shape = shape
        self.rotation = initial_rotation
        self.x = x
        self.y = y

    def 

    def locations(self):
        if self.shape = "L":
            
